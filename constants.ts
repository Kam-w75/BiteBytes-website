// Navigation
export const SCROLL_THRESHOLD = 20;
export const NAV_SCROLL_DELAY = 150;

// Animation
export const ANIMATION_DURATION = 0.6;
export const ANIMATION_DURATION_FAST = 0.3;
export const ANIMATION_OFFSET_X = 20;
export const ANIMATION_OFFSET_Y = 30;

// Form validation
export const MIN_MESSAGE_LENGTH = 10;
export const EMAIL_REGEX = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
